# Local Verification Receipt — Stream Deck Agents V1 (2026-08-06)

Scope: the hook-driven agent session registry — Bun core + SQLite registry +
read-only daemon + official-SDK Stream Deck plugin — verified end to end on one
physical 5x3 deck. This receipt is redacted: no real session identifiers,
prompts, transcript content, credentials, device serials, or unrelated provider
configuration. Synthetic aliases only.

## Commits tested and installed

- Final automated gate (Step 8): ran at HEAD `326fc836e8c9b1f8c9aab820ec24ceb6ff18073c`
  ("Correct the Codex marketplace registration steps"), with only this receipt
  untracked in the working tree.
- Install, daemon-path, and initial-gate evidence (Steps 1, 3, 4): captured at
  `253b02ecb17c50dc205bd65c27af79574add8873` ("Bundle the 15-key Stream Deck
  profile") plus the then-uncommitted `pack:plugin` `--force` fix, committed
  unchanged as `aec1342298915d02326ebd9d65685d0b537a9770` ("Make plugin
  packaging idempotent"). `git diff 253b02e..aec1342` is exactly that one
  `package.json` line — no source change, build outputs unaffected — so the
  installed tree equals `aec1342`. Installed runtime written 2026-08-06 17:36
  local; packaging commands had been pinned reproducible (`--no-update-check`)
  at `d6956fb51e959358cbcc497414b9e806ee30e0d1`.
- Codex hook documentation corrections (two install-time defects): `326fc83`.

## Versions

- bun 1.3.14 — all tests, builds, validation, packaging, and the installer ran under bun
- node v22.23.2 — tooling on PATH (`package.json` declares `>=24`; no impact — nothing in the gate ran under it)
- Node 24.13.1 — Stream Deck app's managed host runtime, observed executing the plugin process
- Stream Deck app 7.5.1.22901 ("Elgato Stream Deck.app")
- @elgato/streamdeck 2.1.0 (official SDK)
- @elgato/cli 1.7.4
- codex-cli 0.146.0

## Automated gate — exact commands and outcomes

Run from the worktree root.

| Command | Outcome |
|---|---|
| `bun test` | PASS — 188 pass, 0 fail, 1463 expect() calls, 11 files |
| `bun run typecheck` | PASS — `tsc --noEmit`, exit 0, no diagnostics |
| `bun run build` | PASS — exit 0; produced core binary `dist/stream-deck-agents` and plugin bundle `com.drewritter.stream-deck-agents.sdPlugin/bin/plugin.js` (one pre-existing, known tslib "this has been rewritten to undefined" warning) |
| `bun run pack:plugin` | PASS — `✔ Validation successful`; `✔ Successfully packaged plugin` |
| `git diff --check` | PASS — silent, exit 0 |

### Final gate re-run (Step 8, at HEAD 326fc83)

| Command | Outcome |
|---|---|
| `bun test` | PASS — 188 pass, 0 fail, 1463 expect() calls, 11 files [514 ms] |
| `bun run typecheck` | PASS — `tsc --noEmit`, exit 0, no diagnostics |
| `bun run build` | PASS — exit 0; core binary and plugin bundle rebuilt (same known tslib warning) |
| `bun run pack:plugin` | PASS — Validation successful; packaged 10 files, 143.5 kB; exactly one package in `dist/` |
| `git diff --check` | PASS — silent, exit 0 |
| `git status --short` | `?? docs/verification/` (this receipt) and `?? node_modules/` (bun install artifact, untracked since the first gate run; not part of the change) |

## Plugin validation and package result

`streamdeck validate` + `streamdeck pack` (via `bun run pack:plugin`):

```
✔ Validation successful
  Name:           Stream Deck Agents
  Version:        0.1.0.0
  UUID:           com.drewritter.stream-deck-agents
  Total files:    10
  Unpacked size:  143.5 kB
  File name:      com.drewritter.stream-deck-agents.streamDeckPlugin
✔ Successfully packaged plugin
```

Exactly one `.streamDeckPlugin` package in `dist/`.

## Install — core, daemon, plugin, profile

`bun run scripts/install-local.ts`: the first attempt failed at the pack step
("File already exists"), exposing a real idempotency defect — `pack:plugin`
lacked `--force`, so any installer re-run aborted, contradicting the installer's
documented idempotent-converge contract and the install-confirmation retry path.
Fixed by the one-line `--force` addition (`aec1342`), verified by packing with a
package already present and by a full installer re-run. Second attempt: PASS —
build, validate, package, LaunchAgent load, plugin/profile install all
converged; "install-local: complete"; provider configuration untouched by the
installer.

Installed paths and modes (`stat -f '%Sp %N'`):

```
drwx------  /Users/drewritter/Library/Application Support/com.drewritter.stream-deck-agents
-rwx------  /Users/drewritter/Library/Application Support/com.drewritter.stream-deck-agents/bin/stream-deck-agents
-rw-------  /Users/drewritter/Library/Application Support/com.drewritter.stream-deck-agents/registry.sqlite3
-rw-------  /Users/drewritter/Library/Application Support/com.drewritter.stream-deck-agents/snapshot-v1.json
-rw-------  /Users/drewritter/Library/LaunchAgents/com.drewritter.stream-deck-agents.plist
```

Directory 0700, executable 0700, database 0600, snapshot 0600, plist 0600 — all
as specified.

LaunchAgent: `gui/501/com.drewritter.stream-deck-agents` — `active count = 1`,
`state = running`, program is the installed executable with argument `daemon`
(re-confirmed running while writing this receipt).

`stream-deck-agents sessions list` → `[]`, exit 0, valid JSON.

## Daemon path with synthetic events (before hooks)

Provider alias `claude`, session alias `verify-synthetic-1`, invented title and
cwd. Each native event object was piped as one JSON object on stdin to the
installed executable (`stream-deck-agents event claude`), exactly as a hook
invokes it; the snapshot was then polled until the expected state was visible,
mode-checked, and schema-validated with the repo's own `parseSessionSnapshot`.

| Native event | Expected | Visible after | Result |
|---|---|---|---|
| SessionStart | idle, slot 1 | ~150 ms | PASS |
| UserPromptSubmit | working | ~250 ms | PASS |
| PermissionRequest | waiting | ~150 ms | PASS |
| StopFailure | error | ~200 ms | PASS |
| Stop | idle | ~150 ms | PASS |
| SessionEnd | removed | ~300 ms | PASS |

Every event: exit 0 (fail-open path), snapshot mode 0600 at every check,
`schemaVersion` 1, health `ok`, slot constant across status changes; all
transitions visible within roughly one 250 ms daemon poll interval. Afterwards
`sessions clear-all` left the snapshot empty and still 0600; the daemon remained
running with zero diagnostic logs (clean run).

## Profile, device, and SDK contexts

- Profile "Stream Deck Agents" (bundled, read-only, auto-installed), selected on the deck.
- Device: one 5x3 Stream Deck, DeviceType 0.
- All 15 cells instantiate the single session-grid action: 15 contexts confirmed, with no duplicates after profile switches.

## Provider hook installation (installed last, per plan)

One provider at a time, each with a pre-change backup preserved until the
physical gate completed; no existing prompts, transcripts, credentials, or
unrelated hook commands were read into this receipt.

- **Claude** — hook entries merged into `~/.claude/settings.json` (10 events;
  pre-existing hook entries preserved untouched; backup kept with a
  `.before-stream-deck-agents` suffix). Two real Claude sessions observed in the
  registry tracking working state. PASS.
- **Kimi** — 9 `[[hooks]]` entries appended to `~/.kimi-code/config.toml`
  (backup made; pre-existing hook entries preserved untouched; exact event names
  kept strict-schema-safe). A real Kimi session observed on the deck after a new
  session start. PASS.
- **Codex** — local plugin `~/.agents/plugins/stream-deck-agents-codex` with a
  marketplace file. Two documentation defects found and fixed in
  `docs/hook-configuration.md` (`326fc83`): `source.path` resolves against the
  registered marketplace root (not the marketplace file's directory), and the
  marketplace itself requires one-time registration. Plugin installed and
  enabled; full lifecycle proven (session appeared mid-turn as working, removed
  at SessionEnd) via a one-off hook-trust-bypass run; persistent hook trust then
  approved through the provider's own review flow and a real Codex session
  observed on the deck. PASS.

**Accepted premise:** that the providers invoke their configured hooks was
accepted, not re-proved.

## Physical device scenarios (Stream Deck 7.5.1.22901 — all human-confirmed PASS)

1. **Status tiles** — one tile each idle (`#94A3B8`), working (`#20B8FF`),
   waiting (`#FFB020`), error (`#FF4D67`), each with the correct animation
   treatment. PASS.
2. **Provider marks** — Claude, Codex, and Kimi marks rendered. PASS.
3. **Nested descendants** — a descendant changed the parent's count badge and
   effective status (child error turned the parent red), then reverted on
   SubagentStop. PASS.
4. **Session removal** — SessionEnd removed a tile without compacting other
   slots. PASS.
5. **Overflow and paging** — 16 synthetic sessions: overflow latch, NEXT key,
   slot 15 on page 2, wrap-around, gap preservation, and clamp back to page 1
   after deleting the overflow sessions. PASS.
6. **Profile switching** — switching away and back produced no duplicate
   contexts and no stuck animation. PASS.
7. **Animation load** — 14 simultaneously animated keys at the approved cadence,
   smooth, with no visible host backlog. PASS.
8. **Missing snapshot** — in-process last-good rendering with the degraded
   indicator; blank offline rendering after a cold plugin restart (process
   kill — note: the `streamdeck restart` deep-link does NOT recycle a linked
   plugin process); self-recovery when the snapshot returned. PASS. The
   explicitly unhealthy-snapshot case is covered by the automated
   daemon/controller tests.

## Defects found and fixed during Task 12

- `pack:plugin` lacked `--force` → installer re-runs aborted. Fixed in
  `aec1342`; verified by double-pack and a full installer re-run.
- The manifest declared a Category without a CategoryIcon; the Stream Deck app
  rejects such plugins while CLI validate passes regardless. Fixed with
  generated PNGs in `253b02e`.
- `docs/hook-configuration.md` Codex marketplace path/registration defects
  (above). Fixed in `326fc83`.

## Accepted observations (not defects)

- The launchd-owned `daemon.stdout.log` / `daemon.stderr.log` are mode 0644 and
  empty by design — launchd owns those redirections; the app's own files
  (database, snapshot, component logs) are all 0600.
- The `streamdeck restart` deep-link does not recycle a linked plugin process;
  a process kill is required for cold-restart testing.
